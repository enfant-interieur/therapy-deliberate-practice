# Local Runtime Gateway

## Speech synthesis

Text-to-speech is temporarily disabled in the local runtime. The desktop helper only
ships LLM and STT models (Qwen3 MLX/HF + Parakeet MLX/Faster Whisper) until a new TTS
backend is ready.
