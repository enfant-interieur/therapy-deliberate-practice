# Copy this file to `.env.release` (and optionally `.env.release.local`) and fill in
# your local signing credentials. The release scripts automatically source these
# files and export the variables for `npm run release*`. Never commit the filled
# file to git.

# macOS codesign certificate (.p12) exported as base64.
# APPSTORE_CERTIFICATES_FILE_BASE64="PASTE_BASE64_HERE"
# Legacy fallback still supported: MAC_CODESIGN_CERT_B64

# Password used when exporting the .p12.
# APPSTORE_CERTIFICATES_PASSWORD="change-me"
# Legacy fallback still supported: MAC_CODESIGN_CERT_PASSWORD

# Common Name of your signing identity (matches Keychain Access).
# Example: Apple Distribution: Therapy Inc (TEAMID1234)
# APPLE_SIGNING_IDENTITY="Apple Distribution: Company Name (TEAMID)"

# Optional: Installer identity when producing signed PKGs.
# APPLE_INSTALLER_IDENTITY="Developer ID Installer: Company Name (TEAMID)"

# Required for notarization/stapling (DMGs and PKGs).
# APPLE_TEAM_ID="ABCDE12345"

# Option 1: Notarize via Apple ID + app-specific password.
# APPLE_ID="name@example.com"
# APPLE_APP_SPECIFIC_PASSWORD="xxxx-xxxx-xxxx-xxxx"

# Option 2: Notarize via App Store Connect API key.
# APPLE_API_KEY_ID="XXXXXXXXXX"
# APPLE_API_ISSUER="YYYYYYYY-YYYY-YYYY-YYYY-YYYYYYYYYYYY"
# Base64 of the AuthKey.p8 file – stored temporarily during the build.
# APPLE_API_KEY_B64="PASTE_BASE64_HERE"

# Windows remote builder connection info
# RELEASE_WINDOWS_HOST="windows-builder.local"
# RELEASE_WINDOWS_USER="builder"
# RELEASE_WINDOWS_REPO_DIR="C:/repos/therapy-deliberate-practice"

# Windows signing certificate (.pfx) exported as base64.
# WINDOWS_CODESIGN_CERT_B64="PASTE_BASE64_PFX"

# Password used when exporting the Windows .pfx file.
# WINDOWS_CODESIGN_CERT_PASSWORD="change-me"

# Subject string used by signtool when signing installers.
# WINDOWS_CODESIGN_SUBJECT="Therapy Inc"
